import { TntStatusPipePipe } from './tnt-status.pipe-pipe';

describe('TntStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TntStatusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
